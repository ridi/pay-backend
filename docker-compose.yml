version: "3"

services:
  apache:
    image: ridi/pay:${TAG}
    container_name: apache
    build:
      context: .
      args:
        SITE: ${SITE}
    ports:
      - 80:80
      - 443:443
    volumes:
      - .:/app
